language: c

branches:
  only:
    - master

before_install:
  - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_1.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :1 -ac -screen 0 1280x1024x16"
  - sleep 3
  - export DISPLAY=:1.0
  - sudo wget http://downloads.arduino.cc/arduino-1.8.6-linux64.tar.xz
  - sudo tar xf arduino-1.8.6-linux64.tar.xz
  - sudo mv arduino-1.8.6 /usr/local/share/arduino
  - sudo ln -s /usr/local/share/arduino/arduino /usr/local/bin/arduino

install:
  - arduino --pref "boardsmanager.additional.urls=http://arduino.esp8266.com/stable/package_esp8266com_index.json" --save-prefs
  - arduino --install-boards esp8266:esp8266 --save-prefs
#  - arduino --install-library "ESP8266WiFi"
#  - arduino --install-library "PubSubClient"

script:
  - arduino --verify --board arduino:avr:uno mqtt-temp-publisher.ino
#  - build_main_platforms
#  - arduino --verify --board esp8266:esp8266
